<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 	
	"-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="miniProject.mapper.WishMapper">
<insert id="wishInsert" parameterType="wish">
    insert into WISH(MEMBER_NUM, GOODS_NUM, WISH_DATE)
    values(#{memberNum}, #{goodsNum}, #{wishDate})
</insert>
<select id="wishSelectList" parameterType="wish">
    select MEMBER_NUM memberNum, GOODS_NUM goodsNum, WISH_DATE wishDate
    from WISH
</select>
</mapper>