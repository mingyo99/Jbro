<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 	
	"-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="miniProject.mapper.PurchaseMapper">

<select id="purchaseSelectOne" resultType="goods">
select goods_main_store goodsMainStore, goods_price goodsPrice, delivery_cost deliveryCost, goods_name goodsName
from goods  
where goods_Num = #{goodsNum}
</select>

</mapper>